{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Estudiosala</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Estudioid</th>
                <td>{{ entity.estudio }}</td>
            </tr>
            <tr>
                <th>Salaid</th>
                <td>{{ entity.sala }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ entity.activo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('estudiosala') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('estudiosala_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('estudiosala_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
