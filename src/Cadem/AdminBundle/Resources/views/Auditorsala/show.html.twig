{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Auditorsala</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Auditorid</th>
                <td>{{ entity.auditor }}</td>
            </tr>
            <tr>
                <th>Salaid</th>
                <td>{{ entity.sala }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ entity.activo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('auditorsala') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('auditorsala_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('auditorsala_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
